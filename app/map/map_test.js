'use strict';

describe('myApp.map module', function() {

  beforeEach(module('myApp.map'));

  describe('map controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var mapCtrl = $controller('cornerCtrl');
      expect(mapCtrl).toBeDefined();
    }));

  });
});